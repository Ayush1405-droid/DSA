#LIFO

stack = [] #create an empty stack

stack.append(1) #push an element onto the stack

stack.append(2) #push another element onto the stack

stack.pop() #pop the top element from the stack

stack[-1] #access the top element of the stack without removing it

len(stack) #get the number of elements in the stack